{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Generated by 'fqe init --with-mutation'. Tune mutate patterns to your codebase. The 'high' / 'low' / 'break' thresholds are coarse Stryker-level gates; the real per-PR threshold check is done by fqe via the Wilson 95% CI upper bound for the configured blast_radius class. See docs/recipes/ai-test-generation.md.",
  "packageManager": "npm",
  "reporters": ["progress", "clear-text", "json"],
  "testRunner": "command",
  "commandRunner": {
    "command": "npm test"
  },
  "mutate": [
    "src/**/*.js",
    "src/**/*.ts",
    "lib/**/*.js",
    "lib/**/*.ts",
    "!**/*.test.js",
    "!**/*.test.ts",
    "!**/*.spec.js",
    "!**/*.spec.ts"
  ],
  "coverageAnalysis": "perTest",
  "thresholds": {
    "high": 90,
    "low": 70,
    "break": 60
  },
  "timeoutMS": 10000,
  "concurrency": 4
}
