{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/booyajones/fqe/schemas/fqe-config.schema.json",
  "title": "fqe .fqe.yml config",
  "description": "Schema for the fqe quality-gate config (.fqe.yml). Enforced fail-closed at run time by cli/lib/config_schema.js. additionalProperties is false everywhere, so a typo'd key (for example 'whne' instead of 'when') is rejected instead of silently disabling a runner and letting the gate pass green.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "version": {
      "description": "Optional config-format version marker.",
      "type": ["string", "number"]
    },
    "runners": {
      "description": "Map of runner name to runner config. An empty map means no runners, which always passes.",
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/runner" }
    }
  },
  "$defs": {
    "runner": {
      "type": "object",
      "additionalProperties": false,
      "required": ["command"],
      "properties": {
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Executable to spawn (no shell). For example 'node', 'bash', 'python3', 'npx'."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Argument vector. ${FQE_COMMIT_SHA}, ${FQE_HEAD_SHA}, and ${FQE_PR_NUMBER} are substituted."
        },
        "when": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Glob patterns. The runner fires when any changed file matches a pattern."
        },
        "required": {
          "type": "boolean",
          "description": "If true the runner must fire and pass. A required runner that does not fire is a FAIL."
        },
        "always_run": {
          "type": "boolean",
          "description": "If true the runner fires on every run regardless of the diff."
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-runner timeout in milliseconds. Defaults to 300000 (5 minutes)."
        }
      },
      "anyOf": [
        {
          "required": ["always_run"],
          "properties": { "always_run": { "const": true } }
        },
        {
          "required": ["when"],
          "properties": { "when": { "type": "array", "minItems": 1 } }
        }
      ]
    }
  }
}
